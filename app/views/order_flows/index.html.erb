<div class="row mt-3">
  <div class="col">
    <div class="d-flex justify-content-between">
      <%= link_to 'Powrót', root_path, class: 'btn btn-sm btn-secondary' %>
      <%= @where_you_are %>
      <%= link_to 'Nowy', new_order_flow_path, class: 'btn btn-sm btn-primary' %>
    </div>
  </div>
</div>
<div class="row mt-3">
  <% @order_flows.each do |order_flow| %>
    <div class="col-md-4 col-sm-12">
      <div class="card m-1 mb-3">
        <div class="card-body">
          <div class="d-flex justify-content-between">
            <h5 class="card-title"><%= order_flow.name %></h5>
            <h5 class="card-title"><%= order_flow.flow_step %></h5>
          </div>
          <%= link_to 'Edycja', edit_order_flow_path(order_flow), class: 'card-link' %>
        </div>
      </div>
    </div>
  <% end %>
</div>