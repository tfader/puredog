<%= form_with model: [@exam_group, @exam_group_exam], autocomplete: :off, local: true do |form| %>

  <div class="form-group">
    <div class="form-text"><%= 'Badanie' %></div>
    <%= form.collection_select :exam_id, @exams.order(:name), :id, :name, include_blank: false,  multiple: false, :class => 'form-control', :option => 'Small select' %>
  </div>

  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-sm btn-primary' %>
      <%= link_to 'Powrót', exam_groups_path(:exam_group_id => @exam_group.id), class: 'btn btn-sm btn-secondary' %>
    </div>
  </div>

<% end %>