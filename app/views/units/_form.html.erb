<%= form_with model: @unit, autocomplete: :off, local: true do |form| %>

  <div class="form-group">
    <label><%= 'Nazwa' %></label>
    <%= form.text_field :name, class: 'form-control', placeholder: 'Nazwa', autofocus: true, presence: true, :required => 'required' %>
  </div>

  <div class="form-group">
    <label><%= 'Kod' %></label>
    <%= form.text_field :code, class: 'form-control', placeholder: 'Kod', presence: true, :required => 'required' %>
  </div>


  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-sm btn-primary' %>
      <%= link_to 'Powrót', units_path, class: 'btn btn-sm btn-secondary' %>
    </div>
  </div>

<% end %>
