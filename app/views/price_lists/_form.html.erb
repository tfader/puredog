<%= form_with model: @price_list, autocomplete: :off, local: true do |form| %>

  <% if @price_list.client.present? %>
    <div class="form-group">
      <label><%= 'Klient' %></label>
      <%= form.text_field :client_id, data: { autocomplete: autocomplete_client_name_clients_path }, 'data-auto-focus' => true, :class => 'form-control select', :option => 'Small select', autofocus: true, value: @price_list.client.name_with_city %>
    </div>
  <% else %>
    <div class="form-group">
      <label><%= 'Klient' %></label>
      <%= form.text_field :client_id, data: { autocomplete: autocomplete_client_name_clients_path }, 'data-auto-focus' => true, :class => 'form-control select', :option => 'Small select', autofocus: true %>
    </div>
  <% end %>


  <div class="form-group">
    <label><%= 'Nazwa' %></label>
    <%= form.text_field :name, class: 'form-control', placeholder: 'Nazwa' %>
  </div>

  <div class="form-group">
    <%= form.check_box :is_cito, class: 'input', id: 'is_cito', type: 'checkbox' %>
    <label><%= 'Cito' %></label>
  </div>

  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-primary' %>
      <%= link_to 'Powrót', price_lists_path, class: 'btn btn-secondary' %>
    </div>
  </div>

<% end %>
