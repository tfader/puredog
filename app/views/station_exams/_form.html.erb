<%= form_with model: [@spot, @station, @station_exam], autocomplete: :off, local: true do |form| %>

  <div class="form-group">
    <div class="form-text"><%= 'Badanie' %></div>
    <%= form.collection_select :exam_id, @exams.all.order(:name), :id, :name_with_group_name, include_blank: false,  multiple: false, :class => 'form-control', :option => 'Small select' %>
  </div>

  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-sm btn-primary' %>
      <%= link_to 'Powrót', spot_station_station_exams_path(@spot, @station), class: 'btn btn-sm btn-secondary' %>
    </div>
  </div>

<% end %>
