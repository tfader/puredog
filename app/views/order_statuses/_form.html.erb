<%= form_with model: @order_status, autocomplete: :off, local: true do |form| %>

  <div class="form-group">
    <label><%= 'Nazwa' %></label>
    <%= form.text_field :name, class: 'form-control', placeholder: 'Nazwa', autofocus: true, presence: true, :required => 'required' %>
  </div>

  <div class="form-group">
    <div class="form-text"><%= 'Etap realizacji zlecenia' %></div>
    <%= form.collection_select :order_flow_id, OrderFlow.all.order(:name), :id, :name_with_step, include_blank: false,  multiple: false, :class => 'form-control', :option => 'Small select' %>
  </div>


  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-sm btn-primary' %>
      <%= link_to 'Powrót', order_statuses_path, class: 'btn btn-sm btn-secondary' %>
    </div>
  </div>

<% end %>
