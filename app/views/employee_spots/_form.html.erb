<%= form_with model: [@employee, @employee_spot], autocomplete: :off, local: true do |form| %>

  <div class="form-group">
    <div class="form-text"><%= 'Użytkownik' %></div>
    <%= form.collection_select :spot_id, Spot.all.order(:name), :id, :name, include_blank: false,  multiple: false, :class => 'form-control', :option => 'Small select' %>
  </div>

  <div class="form-group">
    <label>Od kiedy</label>
    <%= form.text_field :date_from, class: 'form-control', type: 'date' %>
  </div>

  <div class="form-group">
    <label>Do kiedy</label>
    <%= form.text_field :date_to, class: 'form-control', type: 'date' %>
  </div>

  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-sm btn-primary' %>
      <%= link_to 'Powrót', employees_path, class: 'btn btn-sm btn-secondary' %>
    </div>
  </div>

<% end %>
